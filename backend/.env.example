# Backend Environment Variables (Render)
# Copy this to .env locally (do NOT commit real secrets) and configure these in Render’s dashboard.

MONGO_URI=
JWT_SECRET=

# Xendit
XENDIT_SECRET_API_KEY=

# Email
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=
FROM_NAME=
EMAIL_PROVIDER=
BREVO_API_KEY=
DISABLE_EMAIL_SEND=false

# App/Frontend URLs
APP_URL=
FRONTEND_URL=

# PayMongo
# Use either PAYMONGO_API_KEY (live) or PAYMONGO_TEST_SECRET_KEY (test)
PAYMONGO_API_KEY=
PAYMONGO_TEST_SECRET_KEY=
PAYMONGO_PUBLIC_KEY=
# Set to your Render backend URL + /paymongo/webhook
PAYMONGO_WEBHOOK_URL=
# Set after registering webhook
PAYMONGO_WEBHOOK_SECRET=
